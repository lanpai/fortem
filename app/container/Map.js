import React from 'react';
import {
    ScrollView,
    Text
} from 'react-native';

const Map = () => {
    return (
        <ScrollView>
            <Text>Map</Text>
        </ScrollView>
    );
};

export default Map;
